angular.module("str",[])
.controller("startController",function($scope,$state){
    /*$scope.htmlPracticeQuestions = [];
    $scope.htmlPracticeAnswers = [];
    $scope.cssPracQues = [];
    $scope.cssPracAnswer = [];
    $scope.$on("htmlPracQues",function(e,ques){
      alert("Question no is " + ques);
      $scope.htmlPracticeQuestions.push(ques);
    });
    $scope.$on("htmlPracAns",function(e,ans){
      $scope.htmlPracticeAnswers.push(ans);
    });
    $scope.$on("delhtmlPrac",function(ev,del){
      $scope.htmlPracticeQuestions.splice(del,1);
      $scope.htmlPracticeAnswers.splice(del,1);
    });
    $scope.quitHt = function(){
      $state.go("appStart.startPg");
    };
    $scope.$on("eve",function(){
          alert("data from css res");
        })*/
    })